generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder", "multiSchema"]
}

datasource db {
  provider = "postgresql"
  schemas = ["user_schema","billing_schema","cms_schema","ai_schema"]
  url      = env("DATABASE_URL")

}
