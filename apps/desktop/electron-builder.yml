appId: com.saasforge.desktop
productName: SaaS Forge
npmRebuild: false

protocols:
  - name: saas-forge
    schemes:
      - saas-forge

linux:
  target:
    - AppImage
    - deb
    - snap
  category: Development
  # Protocol handler is added to the .desktop file automatically via `protocols` above
  # After install run: xdg-mime default saas-forge.desktop x-scheme-handler/saas-forge

snap:
  confinement: strict
  grade: stable
  summary: "SaaS Forge Desktop App"
  plugs:
    - default
    - network
    - network-bind
    - browser-support
    - desktop
    - desktop-legacy
    - home
    - gsettings
    - opengl
    - x11
    - wayland

mac:
  target: dmg

win:
  target: nsis
